sitemaps:
  default:
    origin: https://main--refresh--cxc38.aem.live
    default: en
    lastmod: YYYY-MM-DD
    languages:
      en:
        source: /query-index.json
        destination: /sitemap.xml
        alternate: /{path}
        hreflang: en
      es:
        source: /us/es/query-index.json
        destination: /us/es/sitemap.xml
        alternate: /us/es/{path}
        hreflang: es
